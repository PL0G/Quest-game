#include <iostream>
#include <cstdlib>
#include <ctime>
#include "windows.h"
#include "Header.h"

entities Hero;
int main() {
	setlocale(LC_ALL, "Russian");
	//Setup();
	//cont();
	/*
	int cem_result = cemetary();
	if (cem_result == 11) {
		
		sell_medalion(1);
	}

	else
		if (cem_result == 12) {
			
			grandD(1);
		}

		else
			if (cem_result == 21) {
				
				sell_medalion(0);
			}

			else
				if (cem_result == 22) {
					
					grandD(0);
				}
	*/
	sell_medalion(1);







	return 0;
}